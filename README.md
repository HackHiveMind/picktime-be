# Picktime Backend

Laravel API backend for the Picktime/iHUB booking platform.

## Workflow

- `main` stays stable.
- Each task starts from `main` on a new branch.
- Each completed task is committed and pushed.

